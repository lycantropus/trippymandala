// Use the global mandalas array
window.mandalas = window.mandalas || [];
let mandalas = window.mandalas;

function createSceneElements() {
  createMandala();
}

function updateSceneElements() {
  updateMandalas();
}
